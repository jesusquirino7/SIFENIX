import ComingSoonPage from "@/components/app/ComingSoonPage";

export default function ProveedoresPage() {
  return (
    <ComingSoonPage
      title="Proveedores"
      description="Alta y gestión de proveedores, siguiendo el mismo patrón que Clientes. Siguiente en la fila una vez validada esa pantalla."
    />
  );
}
